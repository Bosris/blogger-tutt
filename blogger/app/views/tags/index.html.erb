<h1>Tags</h1>

<ul>
    <% @tag.each do |tag| %>
        <li>
            <%= link_to tag.name, tag_path(tag) %>
        </li>
    <% end %>
</u>
<br />
<%= link_to '<< Back to Articles', articles_path %>

